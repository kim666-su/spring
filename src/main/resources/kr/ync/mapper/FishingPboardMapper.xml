<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ync.mapper.FishingPboardMapper">
   
   <sql id="criteria">
      <trim prefix="(" suffix=") AND " prefixOverrides="OR">
         <foreach item='type' collection="typeArr">
            <trim prefix="OR">
               <choose>
                  <when test="type == 'T'.toString()">
                     title like '%'||#{keyword}||'%'
                  </when>
                  <when test="type == 'C'.toString()">
                     content like '%'||#{keyword}||'%'
                  </when>
                  <when test="type == 'W'.toString()">
                     writer like '%'||#{keyword}||'%'
                  </when>
               </choose>
            </trim>
         </foreach>
      </trim>
   </sql>
   
   <select id="getList" resultType="kr.ync.domain.FishingPboardVO">
		select * from tbl_point_board where board_idx > 0 AND H_TITLE LIKE '포인트'
   </select>
   
      <select id="getList1" resultType="kr.ync.domain.FishingPboardVO">
		select * from tbl_point_board where board_idx > 0 AND H_TITLE LIKE '리뷰'
   </select>
   
   <insert id="insert">
 	  insert into tbl_point_board (board_idx, id, title, nick, content, recom_up, recom_down, h_title)
      values (seq_point_board.nextval, #{id}, #{title}, #{nick}, #{content}, #{recom_up}, #{recom_down}, #{h_title})
   </insert>
   
   <insert id="insertSelectKey">

      <selectKey keyProperty="bno" order="BEFORE"
         resultType="Long">
         select seq_board.nextval from dual
      </selectKey>

      insert into tbl_board (bno,title,content, writer, file_1, file_2, file_3)
      values (#{bno},
      #{title}, #{content}, #{writer}, #{file_1}, #{file_2}, #{file_3})
   </insert>
   
   <select id="read" resultType="kr.ync.domain.BoardVO">
      select * from tbl_board where bno =   #{bno}
   </select>


   <delete id="delete">
      delete tbl_point_board where board_idx = #{board_idx}
   </delete>
 
   <update id="update">
      update tbl_point_board
      set title= #{title},
      content=#{content},
      d_date = sysdate
      where board_idx =
      #{board_idx}
   </update>
   
   <!-- <select id="getListWithPaging" resultType="kr.ync.domain.BoardVO">
      <![CDATA[
        SELECT   bno,
              title,
              content,
              writer,
              regdate,
              updatedate
      FROM   (SELECT /*+INDEX_DESC(tbl_board pk_board) */
                  rownum rn,
                  bno,
                  title,
                  content,
                  writer,
                  regdate,
                  updatedate
            FROM   tbl_board
            WHERE   rownum <= 20 
            )
      WHERE rn > 10   
      ]]>
   </select> -->
   <!-- 
   <select id="getListWithPaging" resultType="kr.ync.domain.BoardVO">
      <![CDATA[
        SELECT   bno,
              title,
              content,
              writer,
              regdate,
              updatedate
      FROM   (SELECT /*+INDEX_DESC(tbl_board pk_board) */
                  rownum rn,
                  bno,
                  title,
                  content,
                  writer,
                  regdate,
                  updatedate
            FROM   tbl_board
            WHERE   rownum <= #{pageNum} * #{amount}
            )
      WHERE rn > (#{pageNum} -1) * #{amount}
      ]]>
   </select>
    -->
   <!-- 
   <select id="getListWithPaging" resultType="kr.ync.domain.BoardVO">
      <![CDATA[
         SELECT   bno,
               title,
               content,
               writer,
               regdate,
               updatedate
         FROM   (SELECT /*+INDEX_DESC(tbl_board pk_board) */
                       rownum rn,
                       bno,
                       title,
                       content,
                       writer,
                       regdate,
                       updatedate 
               FROM   tbl_board
               WHERE
      ]]>   
               <trim prefix="(" suffix=") AND " prefixOverrides="OR">
                  <foreach item='type' collection="typeArr">
                     <trim prefix="OR">
                        <choose>
                           <when test="type == 'T'.toString()">
                              title like '%'||#{keyword}||'%'
                           </when>
                           <when test="type == 'C'.toString()">
                              content like '%'||#{keyword}||'%'
                           </when>
                           <when test="type == 'W'.toString()">
                              writer like '%'||#{keyword}||'%'
                           </when>
                        </choose>
                     </trim>
                  </foreach>
               </trim>
      <![CDATA[
               rownum <= #{pageNum} * #{amount}
                  )
         WHERE rn > (#{pageNum} -1) * #{amount}   
        ]]>
   </select>
    -->
   
   <!-- <select id="getListWithPaging" resultType="kr.ync.domain.BoardVO">
      <![CDATA[
      SELECT   bno,
            title,
            content,
            writer,
            regdate,
            updatedate
      FROM   (SELECT /*+INDEX_DESC(tbl_board pk_board) */
                    rownum rn,
                    bno,
                    title,
                    content,
                    writer,
                    regdate,
                    updatedate 
               FROM   tbl_board
            WHERE 
        ]]>
        
      <include refid="criteria"></include> 
      
      <![CDATA[    
               rownum <= #{pageNum} * #{amount}
               )
        WHERE rn > (#{pageNum} -1) * #{amount}   
      ]]>
   </select> -->
   
   <select id="getListWithPaging" resultType="kr.ync.domain.FishingPboardVO">
      <![CDATA[
      SELECT   board_idx,
           	id,
            title,
            d_date,
            nick,
            content,
            recom_up,
            recom_down,
            h_title
      FROM   (SELECT /*+INDEX_DESC(tbl_point_board pk_board) */
                    rownum rn,
                    board_idx,
                    id,
                    title,
                    d_date,
                    nick,
                    content,
                    recom_up,
                    recom_down,
                    h_title
               FROM   tbl_point_board
            WHERE 
        ]]>
      <include refid="criteria"></include> 
      
      <![CDATA[    
               rownum <= #{pageNum} * #{amount}
               )
        WHERE rn > (#{pageNum} -1) * #{amount}   
      ]]>
   </select>
   
   <select id="getList1WithPaging" resultType="kr.ync.domain.FishingPboardVO">
      <![CDATA[
      SELECT   board_idx,
           	id,
            title,
            d_date,
            nick,
            content,
            recom_up,
            recom_down,
            h_title
      FROM   (SELECT /*+INDEX_DESC(tbl_point_board pk_board) */
                    rownum rn,
                    board_idx,
                    id,
                    title,
                    d_date,
                    nick,
                    content,
                    recom_up,
                    recom_down,
                    h_title
               FROM   tbl_point_board
            WHERE 
        ]]>
      <include refid="criteria"></include> 
      
      <![CDATA[    
               rownum <= #{pageNum} * #{amount}
               )
        WHERE rn > (#{pageNum} -1) * #{amount}   
      ]]>
   </select>
   
   <!-- 
   <select id="getTotalCount" resultType="int">
      <![CDATA[
      select count(*) from tbl_board where 
      bno > 0
      ]]>
   </select>
    -->
   <select id="getTotalCount" resultType="int">
      select count(*) from tbl_point_board where 
      
      <include refid="criteria"></include>
      
      <![CDATA[
      board_idx > 0
      ]]>
   </select>
   
   <update id="updateReplyCnt">
      UPDATE tbl_point_board SET replycnt = replycnt + #{amount} WHERE board_idx = #{board_idx}
   </update> 
</mapper>